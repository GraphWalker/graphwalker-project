before_install:
  - find . -name "pom.xml" | xargs sed -i -E -e "0,/<\/parent>/ s/<version>[0-9\.]+-SNAPSHOT<\/version>/<version>${VERSION}<\/version>/g"
  - find . -name "pom.xml" | xargs sed -i -E -e  "0,/<\/parent>/ s/<groupId>org.graphwalker<\/groupId>/<groupId>${GROUP_ID}<\/groupId>/g"
